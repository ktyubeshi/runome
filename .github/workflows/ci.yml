name: ci

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  fast:
    name: Fast checks (placeholder)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.8"]
    steps:
      - uses: actions/checkout@v4
      - name: TODO setup toolchains
        run: |
          echo "Install Rust stable, uv, and Janome==0.5.0 here."
      - name: TODO run smoke tests
        run: |
          echo "Run cargo fmt --check, cargo test -- --ignored, and pytest -k bindings when implemented."

  full:
    name: Full matrix (placeholder)
    needs: fast
    if: false # Enable once the golden + proptest suites are ready.
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.8", "3.10", "3.12"]
    steps:
      - run: echo "TODO: add long-running proptest and diff runs."
